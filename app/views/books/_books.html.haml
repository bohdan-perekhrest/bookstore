- books_set_presenter.set.each do |set_books|
  - set_books.each do |book_presenter|
    .col-xs-6.col-sm-3
      .general-thumb-wrap
        .thumbnail.general-thumbnail
          %img.img-shadow.general-thumbnail-img{src: book_presenter.object.image_url, alt: "design-book_service"}
          .thumb-hover
            .general-thumb-link-wrap
              = link_to book_path(id: book_presenter.object.id), class: 'thumb-hover-link' do
                %i.fa.fa-eye.thumb-icon{'aria-hidden': "true"}
              = link_to order_items_path(order_item: {book_id: book_presenter.object.id, quantity: 1}), method: :post,  class: 'thumb-hover-link' do
                %i.fa.fa-shopping-cart.thumb-icon
      .general-thumb-info
        %p.title= book_presenter.object.name
        %p.font-16.in-gold-500= currency_with_price book_presenter.object.price 
        %span.divider-sm
        %p.lead.small= book_presenter.authors_full_names 
  .row.mb-25

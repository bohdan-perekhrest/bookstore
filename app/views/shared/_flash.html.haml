- flash.each do |type, message|
  .flash-top
    .alert.mg-b-0{class: bootstrap_class_for(type), role:"alert"}
      %button.close{'data-dismiss':"alert", 'aria-label':"Close"}
        %span{'aria-hidden':"true"}×
      = message
